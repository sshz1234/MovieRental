@model IEnumerable<COMP2084Assign2Real.Models.UserRental>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.owingAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.movieTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dueDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.rentalDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.owingAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.movieTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dueDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.rentalDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserRentalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserRentalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserRentalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
